class LayerEngine {
    static handleRaycast(object, interaction) {
        console.log(object)
        if(object.userData.tags.includes(Layer.Tile)) {
            // get tile
            
        }
    }
}